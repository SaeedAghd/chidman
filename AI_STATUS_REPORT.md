# گزارش وضعیت استفاده از هوش مصنوعی در سایت

## 📊 خلاصه وضعیت

### ✅ سرویس‌های AI موجود:
1. **SimpleAIAnalysisService** - تحلیل اصلی فروشگاه‌ها
2. **LiaraAIClient** - کلاینت ارتباط با Liara AI
3. **AIConsultantService** - مشاور AI برای چت با کاربران
4. **LiaraAIService** - سرویس پیشرفته تحلیل با چندین مدل
5. **AdvancedAIManager** - مدیر هوشمند تحلیل‌ها

### ⚠️ وضعیت فعلی:

**در حال حاضر بیشتر تحلیل‌ها rule-based هستند (بدون AI واقعی)**

#### استفاده از AI در SimpleAIAnalysisService:
- **تولید خلاصه مدیریتی**: ✅ استفاده از AI (فقط اگر `LIARA_AI_API_KEY` تنظیم شده باشد)
  - مکان: خط 221-230 در `ai_analysis_service_simple.py`
  - مدل: `openai/gpt-4o-mini`
  - شرط: `if self.ai_client.enabled`
  
- **تحلیل‌های اصلی**: ❌ rule-based (بدون AI)
  - تحلیل چیدمان (layout)
  - تحلیل ترافیک (traffic)
  - تحلیل رفتار مشتری (customer behavior)
  - همه rule-based هستند

#### استفاده از AI در AIConsultantService:
- **چت با مشاور AI**: ✅ استفاده از AI (اگر API key موجود باشد)
  - مکان: `store_analysis/ai_services/ai_consultant_service.py`
  - مدل: `gpt-4.1`
  - شرط: `if self.liara_api_key`

### 🔍 بررسی تنظیمات:

#### در settings.py:
```python
LIARA_AI_API_KEY = os.getenv('LIARA_AI_API_KEY', '')
USE_LIARA_AI = os.getenv('USE_LIARA_AI', 'True').lower() == 'true'
```

#### در LiaraAIClient:
```python
self.api_key = os.getenv("LIARA_AI_API_KEY")
@property
def enabled(self) -> bool:
    return bool(self.api_key)
```

### ⚠️ هشدارها در لاگ:

اگر `LIARA_AI_API_KEY` تنظیم نشده باشد، این warning در لاگ نمایش داده می‌شود:
```
⚠️ LIARA_AI_API_KEY تنظیم نشده است - AI features غیرفعال خواهند بود
```

### 📝 نتیجه‌گیری:

1. **AI فعال است اما محدود**: فقط برای:
   - تولید خلاصه مدیریتی در گزارش‌ها
   - چت با مشاور AI

2. **تحلیل‌های اصلی rule-based هستند**: 
   - بدون استفاده از AI واقعی
   - بر اساس الگوریتم‌های از پیش تعریف شده

3. **برای فعال‌سازی کامل AI**:
   - باید `LIARA_AI_API_KEY` در environment variables تنظیم شود
   - بعد از تنظیم، بخش خلاصه مدیریتی از AI استفاده می‌کند

### 🚀 توصیه:

برای استفاده بیشتر از AI می‌توان:
1. API key را در Liara تنظیم کرد
2. تحلیل‌های اصلی را به AI واگذار کرد (نه فقط rule-based)
3. از چندین مدل AI برای تحلیل‌های مختلف استفاده کرد

