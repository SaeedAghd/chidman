# راهنمای عیب‌یابی مشکل دیپلوی (ECONNRESET)

## 🔍 مشکل
خطای `read ECONNRESET` هنگام دیپلوی به لیارا

## 🎯 علل احتمالی

### 1. مشکل شبکه/اتصال اینترنت
- اتصال اینترنت ناپایدار
- فیلترشکن یا VPN که باعث قطع اتصال می‌شود
- محدودیت پهنای باند

### 2. فایل‌های بزرگ
- فایل‌های ویدیویی (mp4, avi, mov)
- فایل‌های media بزرگ
- فایل‌های staticfiles که نباید دیپلوی شوند

### 3. مشکل با سرور لیارا
- مشکل موقت سرور لیارا
- محدودیت timeout
- مشکل با build cache

## ✅ راه‌حل‌ها

### راه‌حل 1: استفاده از اسکریپت بهبود یافته

```powershell
.\liara_deploy.ps1
```

این اسکریپت به صورت خودکار 3 بار تلاش می‌کند و بین هر تلاش منتظر می‌ماند.

### راه‌حل 2: دیپلوی با گزینه‌های مختلف

#### الف) دیپلوی بدون cache:
```powershell
liara deploy --app chidmano --no-cache
```

#### ب) دیپلوی با debug:
```powershell
liara deploy --app chidmano --debug
```

#### ج) دیپلوی با timeout بیشتر:
```powershell
$env:LIARA_TIMEOUT="600"
liara deploy --app chidmano
```

### راه‌حل 3: بررسی و حذف فایل‌های بزرگ

```powershell
# بررسی فایل‌های بزرگ
Get-ChildItem -Recurse -File | Where-Object {$_.Length -gt 5MB} | Select-Object FullName, @{Name="Size(MB)";Expression={[math]::Round($_.Length / 1MB, 2)}}

# حذف فایل‌های video_output (اگر نیاز نیست)
Remove-Item -Path video_output -Recurse -Force

# حذف فایل‌های staticfiles (دوباره ساخته می‌شوند)
Remove-Item -Path staticfiles -Recurse -Force
```

### راه‌حل 4: بررسی وضعیت Git

```powershell
# بررسی وضعیت
git status

# حذف فایل‌های untracked بزرگ
git clean -fd

# بررسی اندازه repository
git count-objects -vH
```

### راه‌حل 5: دیپلوی تدریجی

اگر پروژه خیلی بزرگ است، می‌توانید از Git LFS استفاده کنید یا فایل‌های بزرگ را جداگانه آپلود کنید.

### راه‌حل 6: بررسی تنظیمات شبکه

```powershell
# تست اتصال به لیارا
Test-NetConnection api.liara.ir -Port 443

# بررسی DNS
nslookup api.liara.ir
```

### راه‌حل 7: استفاده از Git Push (اگر متصل است)

اگر پروژه به Git متصل است، می‌توانید از Git Push استفاده کنید:

```powershell
git push origin main
```

سپس در داشبورد لیارا، Auto Deploy را فعال کنید.

## 🔧 تنظیمات پیشنهادی

### 1. به‌روزرسانی .liaraignore
مطمئن شوید که فایل‌های زیر در `.liaraignore` هستند:
- `video_output/`
- `media/`
- `staticfiles/`
- `*.mp4`, `*.avi`, `*.mov`
- `__pycache__/`
- `.venv/`

### 2. بررسی liara.json
مطمئن شوید که `buildCommand` درست تنظیم شده است.

### 3. بررسی requirements.txt
مطمئن شوید که همه وابستگی‌ها به‌روز هستند.

## 📊 بررسی لاگ‌ها

```powershell
# لاگ‌های دیپلوی
liara logs --app chidmano --follow

# لاگ‌های build
liara logs --app chidmano --type build
```

## 🆘 اگر هیچکدام کار نکرد

1. **بررسی داشبورد لیارا**: به داشبورد لیارا بروید و وضعیت سرور را بررسی کنید
2. **تماس با پشتیبانی**: با پشتیبانی لیارا تماس بگیرید
3. **دیپلوی دستی**: فایل‌ها را به صورت دستی آپلود کنید

## 💡 نکات مهم

- همیشه قبل از دیپلوی، `git status` را بررسی کنید
- فایل‌های بزرگ را در `.liaraignore` اضافه کنید
- از اتصال اینترنت پایدار استفاده کنید
- در صورت امکان، از Git Push استفاده کنید تا Auto Deploy فعال شود

